# Python dependencies for SPICE to .eph conversion
# Install: pip install -r requirements.txt

calceph>=3.5.0      # Multi-format ephemeris reader (SPICE, INPOP, JPL)
numpy>=1.24.0       # Numerical arrays for coefficient fitting
scipy>=1.10.0       # DCT for Chebyshev polynomial fitting
